# Data Pipelines in Natural Language

`nlpipe` compiles natural language through a governed, versioned Pipeline IR into deterministic Apache Airflow DAGs. The model never writes or executes Python.

Implementation and validation are in progress. See the final release validation report before deployment.
